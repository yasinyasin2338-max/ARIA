export default {async fetch(request, env){const u=new URL(request.url);if(u.pathname!='/api/chat'||request.method!='POST')return new Response('ARIA API',{status:200});const {message}=await request.json();const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Authorization':`Bearer ${env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-5',instructions:'تو ARIA هستی؛ یک دستیار فارسی، سریع، صمیمی و هوشمند. پاسخ‌ها را دقیق و مفید بده. محدودیت‌های ایمنی و قوانین را رعایت کن.',input:message})});const j=await r.json();return Response.json({reply:j.output_text||'خطایی رخ داد.'},{headers:{'Access-Control-Allow-Origin':'*'}})}};
